import React from 'react';
// PokemonPageで使用？
export const useSelectPokemon = () => {
  // idを定義しておく必要ある？　クエリーパラメータを取得するやつで　引数でも良い？
  // axiosで個別データを取りに行く
  // ←　こんとき上で定義したidをURLのなかにテンプレート文字列として埋め込む
  // `https://pokeapi.co/api/v2/pokemon/${id}`　これがURL
  return <div></div>;
};
